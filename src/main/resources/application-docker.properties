spring.datasource.url=jdbc:mysql://mysql:3306/${MYSQL_DATABASE}
spring.datasource.username=${MYSQL_USER}
spring.datasource.password=${MYSQL_PASSWORD}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

flyway.url = jdbc:mysql://localhost:${MYSQL_EXTERNAL_PORT}/
flyway.schemas = ${MYSQL_DATABASE}
flyway.user = ${MYSQL_USER}
flyway.password = ${MYSQL_PASSWORD}

openai.api.key=${OPENAI_API_KEY}
telegrambot.token=${TELEGRAM_BOT_TOKEN}